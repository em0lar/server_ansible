http:
  routers:
    nginx-bechilli_de-http:
      rule: "Host(`bechilli.de`) || Host(`www.bechilli.de`)"
      service: "nginx@file"
      entryPoints:
        - "http"
      middlewares:
        - "https_redirect"
    nginx-bechilli_de-https:
      rule: "Host(`bechilli.de`) || Host(`www.bechilli.de`)"
      service: "nginx@file"
      entryPoints:
        - "https"
      tls:
        certResolver: "le_tls"
