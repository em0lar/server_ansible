---
- name: install nginx
  apt:
    pkg: ['nginx']
    state: present

- name: Create vhosts directory
  file:
    path: '/etc/nginx/vhosts'
    mode: 0755
    state: directory
    owner: root
    group: root

- name: Disable default site configuration
  file:
    dest: /etc/nginx/sites-enabled/default
    state: absent

- name: Install vhosts
  block:
    - template: src=vhosts/labcodede.labcode.party.j2 dest=/etc/nginx/vhosts/labcodede.labcode.party
      notify:
      - restart nginx
#
#- name: Install Let's Encrypt ACME conf
#  template: src=acme.conf.j2 dest=/etc/nginx/conf.d/acme.conf

- name: Install config
  template: src=nginx.conf.j2 dest=/etc/nginx/nginx.conf
  notify:
  - restart nginx